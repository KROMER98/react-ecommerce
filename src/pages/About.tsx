import { Container, Alert } from "react-bootstrap";

const About: React.FC<any> = ({ title, text, bg }) => {
  return (
    <Container>
      <h4>Ma page A propos</h4>
    </Container>
  );
};

export default About;
