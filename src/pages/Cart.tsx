import { Container, Alert } from "react-bootstrap";

const Cart: React.FC<any> = ({ title, text, bg }) => {
  return (
    <Container>
      <h4>Ma page Panier</h4>
    </Container>
  );
};

export default Cart;
